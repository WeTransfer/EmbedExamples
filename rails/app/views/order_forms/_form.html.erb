<%= form_with(model: order_form, local: true) do |form| %>
  <% if order_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order_form.errors.count, "error") %> prohibited this order_form from being saved:</h2>

      <ul>
      <% order_form.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity %>
  </div>

  <div class="field">
    <%= form.label :uploaded_material %>
    <div data-widget-host="habitat" id="wt_embed">
      <script type="text/props">
        {
          <%
            # NOTE:
            # The following WtEmbedKey is invalid.
            # Create your own on developers.wetransfer.com/dashboard
          %>
          "wtEmbedKey": "bf6697ab-2299-443a-b58d-60c7df57104a",
          "wtEmbedOutput": "#order_form_uploaded_material"
        }
      </script>
    </div>
    <link rel="stylesheet" href="https://developers.wetransfer.com/embed/bundle.css" />
    <script async src="https://developers.wetransfer.com/embed/bundle.js"></script>
    <%= form.text_field :uploaded_material, hidden: true %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
